{
  "name": "rotary-incluirio",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:css": "tailwindcss -i ./style/input.css -o ./style/bundle.css",
    "build": "npm run build:css && eleventy",
    "serve": "npm run build:css && eleventy --serve",
    "clear": "rm -rf dist",
    "dev": "npm run build:css && eleventy --serve --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "@tailwindcss/cli": "^4.1.13",
    "tailwindcss": "^4.1.13"
  }
}